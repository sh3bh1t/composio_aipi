[project]
name = "composio-aipi"
version = "1.0.0"
description = "AI Product Ops Research System for Composio take-home assignment"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
